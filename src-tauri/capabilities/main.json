{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Capability for the primary window of ARK ASA Config Manager. Grants Tauri core, store, shell and stronghold permissions. The `stronghold:default` permission is added now (P2 of IMPLEMENTATION_PLAN.md) so the plugin only needs to be wired at runtime. `updater:default` will be added in P6 when `tauri-plugin-updater` becomes a dependency.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "core:path:default",
    "core:event:default",
    "core:window:default",
    "core:app:default",
    "core:resources:default",
    "core:menu:default",
    "core:tray:default",
    "core:window:allow-set-title",
    "store:default",
    "shell:allow-spawn",
    "shell:allow-execute",
    "stronghold:default"
  ]
}