import React from 'react'
import SettingsPanel from '../../components/SettingsPanel'

export default function PlayerStatsTab() {
  return <SettingsPanel title="PLAYER STAT MULTIPLIERS"><div className="p-4 text-ark-cyan/60">Player stat settings (in development)</div></SettingsPanel>
}
